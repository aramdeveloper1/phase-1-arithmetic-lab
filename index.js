// // Multiply num1 and num2 to get 62
// let multiply = num1 * num2; // Assuming num1 and num2 are defined elsewhere

// Generate a random integer greater than 0
let random = Math.floor(Math.random() * 10); // Range from 1 to 100

// // Calculate the remainder of dividing num3 by num4 (remainder should be 4)
// let mod = num3 % num4; // Assuming num3 and num4 are defined elsewhere

// Find the highest number in a set (set the set explicitly for this example)
let numbers = [10, 20, 5, 15]; // Replace with your actual set of numbers
let max = Math.max(...numbers); // Using spread operator and Math.max

// console.log("multiply:", multiply);
console.log("random:", random);
// console.log("mod:", mod);
console.log("max:", max);
